@AGENTS.md

## Claude 补充

- 角色：工程负责人。落实 `docs/ART.md` 里「待工程实现」的条目，做完把条目移到「已实现」并附截图说明。
- 动画面相关代码后，用 `npm run shots` 拍改动前后两组截图，自己看一遍再提交。
- 云端会话里 Playwright 已预装（`NODE_PATH=$(npm root -g)` 或 `npm ci` 后可用），不要运行 `playwright install`。
